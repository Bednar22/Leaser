import { AddPoints } from './addPoints';
import { Grid } from '@mui/material';
export const Wallet = (props) => {
    return (
        <>
            <Grid container>
                <Grid item>
                    <AddPoints></AddPoints>
                </Grid>
            </Grid>
        </>
    );
};
